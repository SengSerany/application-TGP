 <br/>
<li><p>De: <%= comment.user.first_name %> <%= comment.user.last_name %> </p>
  <%= comment.content %> 
  <small>Ajouté il y a <%= time_ago_in_words(comment.created_at) %></small>
<hr />
  <%= form_for [comment, Comment.new] do |f| %>
      <%= f.text_area :content, placeholder: "Ajouter un réponse", class: 'form-control' %>

      <%= f.submit "Envoyer", class: 'btn btn-primary mb-2'  %>
      <% end %>
     </form>

  <ul>
      <%= render partial: 'comments/comment', collection: comment.comments %>
  </ul>

</li>